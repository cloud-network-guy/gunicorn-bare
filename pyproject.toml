[project]
name = "gunicorn-bare"
version = "0.1.1"
requires-python = ">=3.11"
dependencies = [
    "gunicorn>=23.0.0",
    "paste>=3.10.0",
    "python-multipart>=0.0.20",
]

[tool.setuptools]
packages = []

